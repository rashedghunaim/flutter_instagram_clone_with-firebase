import 'package:flutter/material.dart';

class ExploredPostDetailsScreen extends StatelessWidget {
  static const routeName = './Explored_PostDetails_Screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Explore'),
        centerTitle: false,
      ),
    );
    
  }
}
